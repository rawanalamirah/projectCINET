{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon src/index.js",
    "db:migrate": "docker compose -f ../docker-compose.yml exec -T db psql -U postgres -d cinet < migrations/001init.sql",
    "db:seed": "docker compose -f ../docker-compose.yml exec -T db psql -U postgres -d cinet < migrations/seed.sql",
    "seed:user": "node scripts/seed-user.js"
  },
  "keywords": [],
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
