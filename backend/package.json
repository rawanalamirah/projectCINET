{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "dev": "nodemon src/index.js",
    "db:migrate": "docker compose -f ../docker-compose.yml exec -T db psql -U postgres -d projectDB < migrations/001init.sql",
    "db:seed": "docker compose -f ../docker-compose.yml exec -T db psql -U postgres -d projectDB < migrations/seed.sql",
    "seed:user": "node scripts/seed-user.js",
    "db:access": "docker compose exec -it db psql -U postgres -d projectDB"
  },
  "keywords": [],
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
